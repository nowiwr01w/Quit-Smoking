dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Stop smoking"

include ':app'
include ':data'
include ':domain'
include ':baseTest'
include ':baseCoroutines'
